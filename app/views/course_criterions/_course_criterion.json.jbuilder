json.extract! course_criterion, :id, :score, :course, :description, :criterion_threshold_id, :course_id, :criterion_id, :created_at, :updated_at
json.url course_criterion_url(course_criterion, format: :json)
