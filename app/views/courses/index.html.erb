<p id="notice"><%= notice %></p>
<div class="row">
  <div class="col-md-6">
    <h1>Courses</h1>

    <table>
      <thead>
        <tr>
          <th>Name</th>
          <th>Description</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @courses.each do |course| %>
          <tr>
            <td><%= course.name %></td>
            <td><%= course.description %></td>
            <td><%= link_to 'Show', course %></td>
            <td><%= link_to 'Edit', edit_course_path(course) %></td>
            <td><%= link_to 'Destroy', course, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <br>

    <button type="button" class="btn btn-default"><%= link_to 'New Course', new_course_path %></button>
  </div>

  <div class="col-md-6">
    <h1>Import Data (.csv)</h1>
    <p>Use these controls to import data into the faculty table in .csv format.
      An example of the correct format for this file can be downloaded <a href="courses_example.csv" download>here</a>.</p>
    <p>
      <%= form_tag courses_import_path, multipart: true do %>
        <button class="btn btn-default btn-file">Choose File<%= file_field_tag :file %></button>
        <%= submit_tag "Import CSV", :class => 'btn btn-default' %>
      <% end %>
    </p>
  </div>
</div>